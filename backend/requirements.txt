# =============================================================
#  LexIR — Backend Python Dependencies
# =============================================================
#  Install:  pip install -r requirements.txt
# =============================================================

# --- Web Server / API ---
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6          # file-upload support for FastAPI

# --- AI / ML ---
groq>=0.4.0                      # Groq LLM client (llama-3.1-8b-instant)
langchain-groq>=0.1.0            # LangChain ↔ Groq integration
langchain-core>=0.1.0            # prompts, output parsers
sentence-transformers>=2.2.0     # all-MiniLM-L6-v2 embeddings
pinecone-client>=3.0.0           # Pinecone vector DB SDK
numpy>=1.24.0

# --- Indian Kanoon + HTTP ---
requests>=2.31.0                 # Indian Kanoon API calls

# --- Utilities ---
python-dotenv>=1.0.0             # .env file loader
pydantic>=2.0.0                  # data validation (FastAPI models)

# --- PDF Parsing (preprocessing scripts) ---
PyPDF2>=3.0.0                    # extract_final.py, extract_bns_and_comparison.py

# --- OCR (optional — only needed for scanned/handwritten FIR upload) ---
# Uncomment the lines below if you need OCR support:
# pytesseract>=0.3.10
# Pillow>=10.0.0
# pdf2image>=1.16.0
